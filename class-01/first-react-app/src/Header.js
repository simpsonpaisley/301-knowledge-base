// Import Other Components or Stylesheets.

import './App.css';

// Style - Not needed, as we have imported a stylesheet.

// Function -

function Header() {
	return <h1>Header!</h1>;
}

// Export

export default Header; // Default is used, as we are only exporting one file.
